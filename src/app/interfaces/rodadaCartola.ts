export interface RodadaCartola {
    anoTemporada: number;
    idRodada: number;
    dtFimInscricao: string;
    hrFimInscricao: string;
    status: string;  // Aberta, Fechada, Encerrada
    valorRodada: number;
}